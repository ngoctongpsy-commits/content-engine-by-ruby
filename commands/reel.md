---
description: Compose a deep-teaching vertical reel bundle (HTML + script + per-platform captions) via reel-production.
---
Compose a reel that teaches the topic in $ARGUMENTS. Use the **reel-production** skill: narrow to ONE concrete
teachable slice, verify every fact, write `script-90s.txt` + a visualized `index.html` from
`knowledge/reel-slide-blocks.md` (obey `knowledge/reel-render-rules.md`), and 4 captions per
`knowledge/per-platform-captions.md`. Save the bundle to `outputs/reels/`. Rendering uses the user's own
HTML->MP4 engine; then hand to **social-distribution**.
